请根据 `ref.typ`（学习其行文）和实际的KiCAD硬件工程 `ref/flight_v2`， 修改 `main.typ` 形成一个专业详细的用户手册。

关于硬件的所有信息都可以在kicad工程中找到，请留意原理图的文本注解，以及具体的网络名称。

用户手册中需要插入一些图片，请在文档中用空白占位，稍后由我补充。

---

**Role:** 你现在是一名资深的嵌入式硬件工程师和技术文档专家，精通使用 Typst 编写高性能、学术级的技术手册。

**Task:**
请参考 `ref.typ` 的视觉风格、排版逻辑和行文语气，通过审计 `ref/flight_v2` 的INAV飞控 KiCAD 工程，将现有的 `main.typ` 升级为一份专业、详尽、具体的硬件/软件使用用户手册，涵盖用户从拿到成品到组装、烧录、连接上位机和实际飞行等的全流程完整指南。

**Requirements:**

1. **深度硬件审计：** * **核心配置：** 提取 MCU 型号、外设资源（传感器的型号与 I2C/SPI 地址）、电源树（LDO/DCDC 的输入输出电压）、动力器件配置（已知有两个3pin的电机接口支持有刷电机和舵机的兼容，不过舵机相关固件待开发，舵机部分标记为未完成。
* **接口定义：** 仔细检查原理图中的网络标签（Net Names）和文本注解，详细列表说明各接线端子（电源、PWM、UART、固件烧录口）的定义，请关注各接口的型号、大体位置、线序等重要特征。
* **设计细节：** 关注 KiCAD 中的 Layer Stackup（层数）、PCB 尺寸以及用户交互元素（LED 状态灯、按键）。
* **用户体验：** 关注用户实际使用流程，按步骤给出使用指南。


2. **内容结构（由浅入深）：**
* **产品概述：** 核心功能与硬件特性。
* **快速入门：** 关键接口的物理连接方式、固件的烧录/升级教程。
* **详细技术规范：** 电气参数与外设地址分配。
* **调试建议：** 基于原理图注解提取的注意事项。


3. **视觉占位：**
* 在涉及 PCB 布局、原理图模块、接线图的位置，使用 Typst 的 `rect` 或特定的方块占位符，并标注明确的图片标题（例如：`#figure(rect(width: 100%, height: 150pt), caption: [PCB Top View 布局图])`）。


4. **写作规范：**
* 保持 `ref.typ` 中的术语一致性。
* 使用表格（table）展示引脚定义，确保清晰易读。
* 语言专业、克制，符合工业级说明书标准。



**Input Files:**

* 格式参考：`ref.typ`
* 硬件源码：`ref/flight_v2` (包括 `.kicad_pcb`, `.kicad_sch`等)
* 待修改文档：`main.typ`

额外信息：

## 烧录流程
- 使用DAP Link给AT32下载占位dummy固件（为了让at32把crsf串口控制权交给外部串口/烧录器）
  - dummy固件通过链接下载(at32-dummy.elf)，链接先留空
  - 该固件将PB4(UART7_TX/ELRS_RX)配置为数字输入，并交替闪烁PC13/PC142个LED
- 使用ELRS Configurator通过串口给ESP8285下载ELRS固件
  - 镊子短接让ESP8285进入串口启动Bootloader
  - ELRS Configurator选择如下配置，无需自定义固件
  - （这是几张elrs configurator的配置截图，请留出占位）
- 使用DAP Link给AT32下载INAV的自定义固件
  - 选择NEUTRONRCF435MINI_FW的target自行编译
  - 或者从链接（先留空）下载 `NEUTRONRCF435MINI_FW.elf`

## 飞行相关流程

按照使用INAV固件的差速控制的无襟翼固定翼飞机，编写总的使用流程。


---

接下来请处理插图。

- pcb-top-view/pcb-bottom-view是用于硬件概览的图，以及pdf是原理图，直接用image()即可嵌入输出pdf。

以下两张图需要额外绘制：

- pcb-top-view2是成角透视的正面图片，左侧是电源接口、上面和下面是pwm电机接口，右上角是uart1 msp接口
- pcb-bottom-view2是背面图片，上面是两行调试用的焊盘，第一行为uart5/uart1，第二行为daplink。请仔细在pcb工程中寻找它们的位置，因为这是由testpoint组成的间距50mil（1.27mm）的焊盘阵列。这张图放在烧录教程中

以上两个图片，请根据pcb文件中的坐标计算大致位置，然后再在图片基础上绘制各个接口的名称和线序等信息，形成清晰的示意图。请使用typst将图片向外扩展出注释区域，并绘制指示线条和文字。

- elrs-config1/2是下载elrs固件的配置截图。
- inav config的连接示意图暂时空缺

---

将处理用的python代码放在assets目录中，另外优化annotation的布局：四周都扩展标注区域；本来位于左边的接口，标注文字就放在左边，原来在上面的就放在上面，而不是挤在右侧。加粗线条和加大文字。文字的排列按照对应连接器的引脚定义来，例如按照多行和多列的方式编写。

---

这是一个mini飞控的typst用户手册工程。请继续修改完善文档：

整体来说，现在写的有点像技术手册，给二次开发的技术人员看很合适，但给客户来说，他们主要关心的是：1. 我拿到之后，怎么接线到电脑上配置（要怎么装inav configurator, 下载地址是啥） 2. 接上去之后，我要玩差速纸飞机，我要怎么配 3. 我怎么对频

请另外开一段快速上手章节，详细描述上述新用户最想知道的东西。

对于大多数用户，由于我们会提前在AT32和esp8285烧录好inav和elrs固件，我们建议用wifi烧写 elrs ，而不是用串口。不过inav固件仍然需要有线烧录（6pin 1.25mm烧录夹夹在测试点焊盘上，注意确认线序）。

---

强调下给inav烧录需要烧录夹；imu型号不是ICM-42688-P而是LSM6DSOWTR；将最后和没有图号的图用易读的格式放在合理的位置上；
澄清在使用串口连接上位机时不需要锂电池供电，使用vbat连接usb串口的5v供电即可；

这是一个mini飞控的typst用户手册工程。请继续修改完善文档：
请关注文中我用注释给出的批注，并修改
添加了assets/annotation_03.png，其中绘制了usb-ttl到sh1.0连接器的接线图。请插入文档
注意：如果需要列出文件，请忽略ref目录下的内容

---

快速开始和进阶设置有内容重合。请将必要的步骤放在快速开始中，例如电机的接线。请先给出你的修改意见，由我审核

---

这是一个mini飞控的typst用户手册工程。请阅读文档仔细分析，当前文档在新手友好、内容完整等方面是否能继续优化？先不管todo里列出的部分，给出你的意见。

请根据你的意见修改，但不需要配件清单与购买建议。另外请在快速开始中加上对频密码（出厂固件默认123456）相关说明。

---

请根据如下意见，继续修改文档：

原理图在给C端客户的使用说明里不用展示  2. 对频部分错误比较多 3.  Ouput配置（配置为Brushed) 4. Mixer 配置写成了Output配置 5. 提示让用户第一次上电配置时不要接电机（因为比如选择airplane，默认输出会使用电调，接了电机电机会转，然后电脑USB会保护，导致上位机断开）

---

请继续修改完善文档`main.typ`：明确飞控产品名称为 LightFin ，来自「座头鲸工作室/HumpbackLab」。

请一并更新文档中其他的非明确指代的产品称呼，确保称呼统一。